{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "bazel: run tests",
            "type": "shell",
            "command": "bazel test :all",
            "group": {
                "kind": "build",
            }
        },
        {
            "label": "bazel: run tests (debug)",
            "type": "shell",
            "command": "bazel test -c dbg :all",
            "group": {
                "kind": "build",
            }
        }
    ]
}